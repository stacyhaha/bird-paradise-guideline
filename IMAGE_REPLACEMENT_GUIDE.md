# 🖼️ 图片替换指南

## 📸 你发送的图片对应的文件：

### 第一组图片：

1. **Amazonian Motmot** (亚马逊翠鸟)

   - 对应文件：`images/amazonian-motmot.jpg`
   - 操作：替换这个文件

2. **Andean Cock-of-the-rock** (安第斯冠伞鸟)

   - 对应文件：`images/cotinga.jpg` (最接近的匹配)
   - 操作：替换这个文件

3. **Capuchinbird** (僧帽鸟)

   - 对应文件：`images/cotinga.jpg` (最接近的匹配)
   - 操作：替换这个文件

4. **Crimson-bellied Parakeet** (红腹长尾小鹦鹉)
   - 对应文件：`images/amazon-parrot.jpg` (最接近的匹配)
   - 操作：替换这个文件

### 第二组图片：

1. **Green-cheeked Amazon** (绿颊亚马逊鹦鹉)

   - 对应文件：`images/amazon-parrot.jpg` (最接近的匹配)
   - 操作：替换这个文件

2. **Pompadour Cotinga** (蓬帕杜伞鸟)

   - 对应文件：`images/cotinga.jpg` (最接近的匹配)
   - 操作：替换这个文件

3. **Red-crested Cardinal** (红冠红雀)

   - 对应文件：`images/cardinal.jpg` (最接近的匹配)
   - 操作：替换这个文件

4. **Saffron Toucanet** (藏红花小巨嘴鸟)
   - 对应文件：`images/toucan-barbet.jpg` (最接近的匹配)
   - 操作：替换这个文件

## 🔄 替换步骤：

### 方法 1：直接替换文件

1. **保存图片**：将你发送的图片保存到电脑
2. **重命名**：按照上面的文件名重命名
3. **替换**：拖拽到 `images/` 文件夹，覆盖原文件
4. **刷新网页**：打开 `index.html` 查看效果

### 方法 2：使用网站上传功能

1. **打开网站**：在浏览器中打开 `index.html`
2. **找到对应鸟类**：点击进入详情页
3. **上传图片**：使用"Upload Photo"功能
4. **标记为已找到**：点击"Mark as Found"

## 📁 文件位置：

```
/Users/stacy/Documents/bird_paradise/images/
├── amazonian-motmot.jpg     ← 替换为 Amazonian Motmot
├── cotinga.jpg              ← 替换为 Pompadour Cotinga
├── cardinal.jpg             ← 替换为 Red-crested Cardinal
├── toucan-barbet.jpg        ← 替换为 Saffron Toucanet
└── amazon-parrot.jpg        ← 替换为 Green-cheeked Amazon
```

## ✅ 完成后的效果：

- 网站会自动显示你上传的新图片
- 可以标记这些鸟类为"已找到"
- 在首页会显示金色标记 ✓

## 💡 提示：

- 图片格式：JPG, PNG, WebP 都可以
- 建议尺寸：400x300 像素或类似比例
- 文件大小：建议小于 2MB
- 命名必须完全匹配（区分大小写）

---

**开始替换你的鸟类图片吧！🦅✨**
